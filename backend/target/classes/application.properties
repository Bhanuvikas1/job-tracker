spring.application.name=job-tracker-backend

server.port=8080

# --- MySQL (update username/password if needed) ---
spring.datasource.url=jdbc:mysql://localhost:3306/jobs_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=bhanuvikas17AB@

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Keep session cookie usable from Vite (5173)

